# 2026-07-18

## Sessions

### 00:15 — Cross-device docs across Foundation + kits + website

- Goal: started as "rewrite getting-started in repo README + shane.logsdon.io";
  user redirected to the real goal — document the missing **cross-device
  (desktop/mobile) behavioral** setup, and make it apply to the three kits too,
  not just the Foundation.
- Did:
  - Foundation `README.md` — new **Working across devices** section (Remote
    Control + Dispatch, iCloud "keep downloaded" gotcha, Android/Windows sync
    alternatives, sync-delay caution, worktree isolation, desktop-only push).
  - Verified Remote Control / Dispatch feature names via the claude-code-guide
    agent before documenting (avoided inventing setting names).
  - Website `loop-and-gate.php` — matching "working across devices" section,
    then a second pass: app-name rename, plugin-first Foundation install (step
    03), new Obsidian step, optional mobile-app note, `#working-across-devices`
    anchor.
  - Audit of the whole doc surface caught the same issues in 8 more files:
    stale "Claude Code Desktop app" naming everywhere, and the Foundation
    `GETTING-STARTED.md` promising mobile sync it never set up ("no setup to
    run"). Reworked it to plugin-first with a vault-placement step; renamed the
    app across all 5 repos; added cross-device pointers from all 3 kits.
- Outcome: done. 6 commits across 5 repos (2 pushed batches + this reflection).
  loop-and-gate-foundation `dbe3bf6`, `f63ff84`; kits `23233f4`/`3d8c624`/`4cabc33`
  (+ earlier `3b59756`/`e2fe421`/`449b43b`); website `2e75844`→`29598a7`.
- Open: dependabot flagged 7 vulns (6 high, 1 low) on shane.logsdon.io —
  unrelated to docs, wants a separate pass. Nothing else.

### 00:55 — Launch strategy research for Loop & Gate

- Goal: research where/when/how to launch the Loop & Gate stack wide, gated on
  an initial-feedback round. Decisions locked with the user: model = **free now,
  paid later**; goal = **audience → activated adoption** (reach is a means).
- Did:
  - Researched current (mid-2026) channel landscape via web — plugin discovery
    surfaces (claude-plugins-official/-community, claudemarketplaces.com),
    community hubs (r/ClaudeCode ~4.2k weekly, r/ObsidianMD), launch-board
    sequencing (Dev Hunt → PH/Show HN after users vouch).
  - Wrote [[launch-strategy]] — wedge (judgment gates, not more autonomy),
    Phase-0 feedback gate w/ exit criteria, tiered channels, launch-week
    sequence, metrics (activated installs > stars), risks, and a ranked
    free→paid tier menu (course + DWY first, team/sync SaaS after).
  - Wrote [[launch-assets]] — demo shot-list (Build kit, the gate-stops-and-asks
    money shot) + install-tutorial outline.
  - Caught that the proposed launch essay ~70% duplicated the live tease post
    "Building on the Margins" — user confirmed that post is a deliberate tease.
    Killed the essay; swapped to demo + install tutorial + repurpose the tease.
- Outcome: done. Two Knowledge notes created; no code/docs shipped (research +
  planning session).
- Open: when execution starts — record the demo, write the install tutorial,
  add email capture to the landing page. Dependabot vulns still open from prior.

### 07:17 — Attribution audit across the launch stack

- Goal: doubt-check that dependency projects + intellectual influences are
  properly attributed everywhere before a wide launch.
- Did:
  - Audited all 5 repos + landing page for influence/dep credit. Found the
    influences (Karpathy, Osmani, Forrest Chang, Huntley, Carson, Lilian Weng)
    well-credited in the Foundation, and superpowers/agent-skills credited by
    handle across build kit + landing.
  - Corrected a wrong assumption: I first flagged `ponytail` as Shane's private
    skill. Shane corrected — ponytail/worktrunk/caveman are **community tools**.
    Found the real upstreams from his GitHub stars: ponytail→DietrichGebert,
    worktrunk→max-sixty, caveman→JuliusBrussee.
  - Checked the iCloud Obsidian vault's original design docs (`/Projects/Loop &
    Gate/`) via the obsidian interface — confirmed the framework's thesis is
    Shane's own three concepts (internal), external set is what needs credit.
  - Shipped: website `loop-and-gate.php` "built on" section (influences +
    community tools) `59fbe64`→source; Grow + Accountability READMEs now
    reference the Foundation for method lineage (`7084c6a`, `bbe4f80`)→main.
  - Captured [[launch-attribution]] — the upstream map + influence anchors +
    the no-vendoring/no-license-obligation conclusion.
- Outcome: done. Attribution complete across the launch surface. 3 repos pushed.
- Open: dependabot re-flagged on the website push (6 high, 1 low) — still a
  separate pass.
