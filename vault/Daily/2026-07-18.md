# 2026-07-18

## Sessions

### 00:15 — Cross-device docs across Foundation + kits + website

- Goal: started as "rewrite getting-started in repo README + shane.logsdon.io";
  user redirected to the real goal — document the missing **cross-device
  (desktop/mobile) behavioral** setup, and make it apply to the three kits too,
  not just the Foundation.
- Did:
  - Foundation `README.md` — new **Working across devices** section (Remote
    Control + Dispatch, iCloud "keep downloaded" gotcha, Android/Windows sync
    alternatives, sync-delay caution, worktree isolation, desktop-only push).
  - Verified Remote Control / Dispatch feature names via the claude-code-guide
    agent before documenting (avoided inventing setting names).
  - Website `loop-and-gate.php` — matching "working across devices" section,
    then a second pass: app-name rename, plugin-first Foundation install (step
    03), new Obsidian step, optional mobile-app note, `#working-across-devices`
    anchor.
  - Audit of the whole doc surface caught the same issues in 8 more files:
    stale "Claude Code Desktop app" naming everywhere, and the Foundation
    `GETTING-STARTED.md` promising mobile sync it never set up ("no setup to
    run"). Reworked it to plugin-first with a vault-placement step; renamed the
    app across all 5 repos; added cross-device pointers from all 3 kits.
- Outcome: done. 6 commits across 5 repos (2 pushed batches + this reflection).
  second-brain-agent `dbe3bf6`, `f63ff84`; kits `23233f4`/`3d8c624`/`4cabc33`
  (+ earlier `3b59756`/`e2fe421`/`449b43b`); website `2e75844`→`29598a7`.
- Open: dependabot flagged 7 vulns (6 high, 1 low) on shane.logsdon.io —
  unrelated to docs, wants a separate pass. Nothing else.
